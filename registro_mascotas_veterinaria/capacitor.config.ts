import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'registro_mascotas_veterinaria',
  webDir: 'www'
};

export default config;
